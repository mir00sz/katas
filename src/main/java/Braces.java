

public class Braces {



    //vdsvdsvdv
    //)))))

    //()(())
    //{{{}
    //[[[]]]]] //bad


public boolean solution(String string) {

    /*Stack<Character> braces = new Stack<>();
    for (char c : string.toCharArray()) {
        if (braces.isEmpty() || isOpenningBracket(c)) {
            braces.add(c);
        } else {
            char last = braces.peek();
            if(doesMatch(c, last)) {
                braces.pop();
            } else {
                return false;
            }
        }
    }

    return braces.isEmpty();


}

    private boolean isOpenningBracket(char c) {
        String openning = "([{<";
        return openning.co;
    }*/

    return true;

}
}
